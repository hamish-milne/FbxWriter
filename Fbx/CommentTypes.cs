namespace Fbx
{
	/// <summary>
	/// Specifies the different kind of comments you can add.
	/// </summary>
	public enum CommentTypes
	{
		Header, // A header comment has a space and a divider line beneath it.
		Inline, // An inline comment has no space and no divider line. Used for example above object connections.
	}
}
