namespace Fbx.PropertyBlocks
{
	/// <summary>
	/// Sub-types for double-precision properties. Not sure what they mean, but I noticed they exist.
	/// </summary>
	public enum DoubleTypes
	{
		Default,
		H,
	}
}
